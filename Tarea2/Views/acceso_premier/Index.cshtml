@model IEnumerable<Tarea2.Models.acceso_premier>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.persona.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pelicula.clasificacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.persona.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pelicula.clasificacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idapremier }) |
            @Html.ActionLink("Details", "Details", new { id=item.idapremier }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idapremier })
        </td>
    </tr>
}

</table>
